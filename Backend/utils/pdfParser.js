import { errorHandler } from "./errorHandler"

export const pdfParser = (report) => {
    try {
         if (!report.url) throw new Error("PDF URL is missing in the report");

         

    } catch (error) {
        
    }
}